# Legacy generated output

These MAT files and CSV trees predate the reviewed `reference-data/v1` contract. They are kept
only for historical diagnosis and are not trusted inputs. New generators and consumers
intentionally reject this directory.

Running the historical mechanism scripts may recreate `Mechanisms/**/CSVOutput` or
`Mechanisms/**/Mechanism.mat`; those regenerated files are ignored and must not be committed.
